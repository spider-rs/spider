[package]
name = "benches"
version = "0.0.0"
publish = false
edition = "2021"

[dependencies]
spider = { version = "1.8.1", path = "../spider" }
criterion = { version = "0.3.5", features = ["html_reports"] }

[[bench]]
name = "crawl"
path = "crawl.rs"
harness = false